export const environment = {
  production: true,
  // Browser runs on the host, so `localhost:8000` reaches the backend
  // exposed port from docker-compose.
  apiBaseUrl: 'http://localhost:8000',
};
